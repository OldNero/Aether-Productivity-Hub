{
  "name": "aether-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:init": "wrangler d1 execute aether-db --local --file=./schema.sql"
  },
  "dependencies": {
    "@lucia-auth/adapter-sqlite": "^3.0.0",
    "hono": "^4.1.0",
    "lucia": "^3.0.0",
    "oslo": "^1.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240320.1",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
